import time
import random

def detect_anomalies(image_path: str) -> float:
    """
    Stub for Anomaly Detection model.
    Returns a score between 0 and 1.
    """
    time.sleep(1) # Simulate inference
    return round(random.uniform(0.1, 0.95), 2)
